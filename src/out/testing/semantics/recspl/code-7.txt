main
num V_a, num V_b, text V_c,
begin
    skip;
end
num F_calculate(V_d, V_e, V_f)
{
    num V_g, num V_h, text V_i,
    begin
        skip;
        F_calculated(V_v, V_w, V_x);
    end
}
    void F_calculates(V_j, V_k, V_l)
    {
        num V_m, text V_n, text V_o,
        begin
            skip;
        end
    }
        void F_calculated(V_s, V_t, V_u)
        {
            num V_p, text V_q, text V_r,
            begin
                skip;
            end
        }
        end
    end
end
